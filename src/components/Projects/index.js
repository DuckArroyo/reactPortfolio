//! Projects list similar to Photolist

import React, { useState } from "react";
//import Modal from "../Modal";

const ProjectsList = () => {
  //States

  //Project array
  const [projects] = useState([
    {
      name: "project name",
      category: "Some Category",
      description: "Lorem",
    },
  ]);

  //filter

  //modal function

  return (<div></div>);
};

export default ProjectsList;
